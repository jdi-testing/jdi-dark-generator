{{>licenseInfo}}

package {{package}};

import com.epam.http.response.RestResponse;
import org.testng.annotations.BeforeClass;
import org.testng.annotations.Test;

import static com.epam.http.requests.ServiceInit.init;

/**
 * API tests for {{classname}}
 */
public class {{classname}}Test {

    private {{classname}} service;

    @BeforeClass
    public void before() { service = init({{classname}}.class); }

    {{#operations}}{{#operation}}
    /**
     * {{summary}}
     * {{notes}}
     */
    {{#consumes}}
    @Test
    public void {{operationId}}{{enumMedia}}Test() {
        // TODO: add call parameters and test validations
        RestResponse resp = {{classname}}.{{operationId}}{{enumMedia}}.call();
        resp.isOk();
    }
    {{/consumes}}
    {{^consumes}}
    @Test
    public void {{operationId}}Test() {
        // TODO: add call parameters and test validations
        RestResponse resp = {{classname}}.{{operationId}}.call();
        resp.isOk();
    }
    {{/consumes}}
    {{/operation}}{{/operations}}
}
